---
import knotSvg from "../../assets/icons/section-divider-knot.svg?raw";

interface Props {
    class?: string;
}

const { class: className = "text-brand-surface w-full max-w-xs mx-auto" } =
    Astro.props;
---

<div class={className} set:html={knotSvg} />
